<?xml version="1.0" encoding="UTF-8"?>
<configuration>
<!--    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>-->
<!--    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>-->

<!--    <springProperty scope="context" name="appName" source="spring.application.name"/>-->
<!--    <springProperty scope="context" name="logstashHost" source="logstash.host" defaultValue="127.0.0.1"/>-->
<!--    <springProperty scope="context" name="logstashPort" source="logstash.port" defaultValue="5044"/>-->

    <!-- Logstash TCP Appender -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
<!--        <destination>${logstashHost}:${logstashPort}</destination>-->
        <destination>localhost:5044</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
<!--            <customFields>{"app":"${appName}", "environment":"${spring.profiles.active:-default}"}</customFields>-->
<!--            <includeContext>false</includeContext>-->
<!--            <timeZone>UTC</timeZone>-->
        </encoder>
<!--        <keepAliveDuration>5 minutes</keepAliveDuration>-->
<!--        <connectionTimeout>5000</connectionTimeout>-->
<!--        <reconnectionDelay>3000</reconnectionDelay>-->
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOGSTASH"/>
    </root>

</configuration>
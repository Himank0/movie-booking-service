server:
  port: 8081
  servlet:
    context-path: /api

  spring:
    application:
      name: movie-booking-service
    mvc:
      static-path-pattern: "/**"
    web:
      resources:
        add-mappings: false
  kafka:
    bootstrap-servers: localhost:9092
#    consumer:
#      group-id: movie-service-group
#      auto-offset-reset: earliest
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
#      properties:
#        spring.json.trusted.packages: "com.moviebooking.movie_booking_service.events"
#        spring.json.type.mapping: ticketBookedEvent:com.moviebooking.movie_booking_service.events.TicketBookedEvent
#        spring.json.value.default.type: com.moviebooking.movie_booking_service.events.TicketBookedEvent
#        spring.json.use.type.headers: false
#    producer:
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
#      properties:
#        spring.json.type.mapping: ticketBookedEvent:com.moviebooking.movie_booking_service.events.TicketBookedEvent
#        spring.json.add.type.headers: false

  data:
    mongodb:
      uri: mongodb://localhost:27017/moviebooking_db  #data base name, to start mongod , ctrl+c
#       uri: mongodb://mongo:27017/moviebooking_db  #data base name, to start mongod , ctrl+c
    jackson:
      serialization:
        indent-output: true

    info:
      app:
        name: Movie Booking App
        version: 1.0.0
        description: Full-stack movie booking platform

    logging:
      level:
        root: INFO
        org.springframework.web: DEBUG
        com.moviebooking.movie_booking_service: DEBUG
